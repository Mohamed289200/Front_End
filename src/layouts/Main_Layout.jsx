import { Outlet } from "react-router-dom";

const Main_Layout = () => {
  return (
    <div>
      <div>ana el main layout ya wald</div>
      <Outlet />
    </div>
  );
};

export default Main_Layout;
